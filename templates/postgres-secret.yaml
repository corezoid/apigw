{{- if .Values.global.db.secret.create }}
---
apiVersion: v1
kind: Secret
metadata:
  name: {{ include "postgres.secretName" . | quote }}
  labels:
    app: {{ .Values.global.product }}
    tier: "postgres"
    release: {{ .Release.Name | quote }}
type: Opaque
data:
  {{- range $key, $value := .Values.global.db.secret.data }}
  {{ $key }}: {{ $value | b64enc | quote }}
  {{- end }}
{{- end}}
